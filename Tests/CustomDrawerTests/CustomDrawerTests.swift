    import XCTest
    @testable import CustomDrawer

    final class CustomDrawerTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
           // XCTAssertEqual(CustomDrawer().text, "Hello, World!")
        }
    }
